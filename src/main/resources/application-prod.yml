spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:mysql://connectdb.cpgqkmy0sm0o.ap-northeast-2.rds.amazonaws.com:3306/connectserver
    username: root
    password: 12341234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jwt:
    secret: ${JWT_SECRET:change-this-to-a-very-long-random-secret}
    expiration-ms: 3600000   # 1시간